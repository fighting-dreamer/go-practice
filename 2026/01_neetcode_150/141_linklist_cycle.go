package main

// linklist cycle is known
// using slow and fast pointer
// when they meet => cycle or if the fast one ends in nit => no cycle
// T point of cycle => after meeting, slow start from head and fast and slow both move one stap a time,
// their meeting point is start of cycle
