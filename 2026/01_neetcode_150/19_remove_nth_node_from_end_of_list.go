package main

// remove nth node from end of list
// two points, first one already n steps ahead, if possible, as if list length is > n
// both pointer then start moving at same pace, when first one reaches the end => the second one is at nth node
// remove the second node.
