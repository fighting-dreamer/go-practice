package main

// validate binary search tree : keep min and max value possible at each node
// for root node, it is INT_MIN and INT_MAX respectively
// each node value must lie in between corresponding min and max value for that node.
