package main

// not taking up now, its similar to chec subtree
// if headA == nil && headB 1= nil || vice versa => not same
// headA.Val == headb.Val && match(headA.Left, headB.Left) && match(headA.right, headB.right)
